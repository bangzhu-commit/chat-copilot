---
name: chat-copilot
description: 实时对话 AI 副驾。适用于直播主持、访谈采访、招聘面试、求职面试、销售谈判、相亲约会、AI 应用评审、口播录制和培训教学，通过实时语音转写、发言人角色映射、场景化提示、评分表和面试复盘辅助对话推进。
---

# Chat Copilot

## 何时使用

当用户需要一个实时对话辅助工具，或提到以下场景时使用：

- 直播、播客、访谈、采访中的实时追问
- HR 或面试官对候选人回答做 STAR 追问
- 求职者根据 HR 问题获得回答结构和简历素材提醒
- 销售会谈、商务谈判、续约沟通中的事实洞察和风险提醒
- 相亲、初次约会、线上语音相亲中的破冰、共鸣、转场和边界提醒
- 公司内部 AI 应用比赛、Demo Day、项目复赛中的评审观察、追问建议和参考评分表
- 口播录制、培训教学中的内容延展和听众问题模拟
- 需要在实时转写中区分「说话人 1 / 说话人 2」，并映射成「嘉宾 / 主持人」「客户 / 我方」等角色的多人对话场景

## 输入资料

工具支持上传 `.txt` / `.md` 作为上下文。上传资料会被加入场景 Prompt，帮助 AI 输出更贴近当前对话的提示。

销售谈判建议资料：

```markdown
客户/嘉宾：姓名、角色、公司、权限范围
我方目标：希望达成什么下一步
底线：价格、交付、付款、不可承诺事项
方案：产品卖点、案例、报价区间
已知信息：痛点、预算、竞品、历史沟通
```

求职面试建议资料：

```markdown
目标岗位：岗位 JD 和关键要求
候选人资料：简历、项目经历、优势
重点素材：想强调的项目、数据、协作角色
表达边界：不能夸大的经历、还没做过的能力
目标公司：业务、产品、面试岗位背景
```

相亲约会建议资料：

```markdown
对方基本信息：职业、城市、兴趣、介绍人提供的信息
自己想展示的真实信息：兴趣、生活方式、价值观
本次目标：轻松认识、判断是否继续约、了解生活节奏
禁区：不想聊的话题、不要显得太功利的问题
```

AI 应用评审建议资料：

```markdown
项目名称：
解决的问题：面向谁，为什么重要
核心功能：最重要的 3 个功能
AI 用在哪里：问答、总结、生成、识别、检索或自动化
当前完成度：Demo、可试用、已上线或稳定使用
效果证据：数据、案例、使用反馈、节省时间
风险和边界：隐私、权限、错误输出、成本和安全
评委关注点：希望评委重点看什么
```

## 场景选择

- `直播主持`：给主持人生成自然追问。
- `访谈采访`：追细节、追故事、追方法。
- `招聘面试（面试官）`：用 STAR 法则帮助面试官追问候选人。
- `求职面试`：识别 HR 问题意图，提示 STAR/CAR/PAR 结构、可用素材和风险。
- `销售谈判`：用 SPIN、MEDDICC、BATNA/ZOPA 和原则式谈判输出事实、风险、推荐动作和谈判提醒。
- `相亲约会`：帮助用户自然破冰、接住情绪、追问具体经历、适度自我披露，并提醒敏感边界。
- `AI 应用评审`：帮助评委记录证据、识别疑点、准备追问，并基于完整转写和项目资料生成参考评分表。
- `口播录制`：提示补案例、类比、数据或收束。
- `培训教学`：模拟学员视角提出问题。

## 输出边界

- 求职面试场景禁止编造经历、数据和项目成果。
- 销售谈判场景必须区分事实和推断；只把对方明确说出的内容标为事实。
- 相亲约会场景不做 PUA、不诱导隐私、不编造人设、不建议施压或假装共情。
- AI 应用评审场景只做评审助理，不直接决定名次；没有证据的口号、效果和数据只能标为待验证。
- 相亲约会默认不鼓励线下相亲时明显盯屏使用；更适合线上语音相亲、相亲前演练、相亲后回顾，或双方知情的实时转写场景。
- 不替用户做最终承诺，不建议单向让价；需要让步时应提醒换取对等条件。
- 实时提示要短，适合用户在对话中快速扫一眼。
- 发言人标签依赖 ASR 模型返回的 speaker 信息；界面会常驻显示角色映射状态，模型未返回 speaker 时保持普通转写，返回后可让用户手动纠正一次角色。
- 外放播客测试容易产生回声和重复识别；工具会做重复过滤和短片段合并，但更推荐用虚拟声卡或真实多人对话测试。

## 验证方式

1. 启动应用并打开 `http://localhost:3000`。
2. 切换到 `销售谈判`，上传客户资料，手动触发后应看到 `[事实]`、`[风险]`、`[推荐]` 或 `[谈判]` 标签。
3. 切换到 `求职面试`，上传简历/JD，手动触发后应看到 `[问题]`、`[考察点]`、`[结构]`、`[素材]` 或 `[风险]` 标签。
4. 切换到 `相亲约会`，输入短对话后应看到 `[破冰]`、`[共鸣]`、`[追问]`、`[自我披露]`、`[转场]` 或 `[边界]` 标签。
5. 切换到 `AI 应用评审`，输入项目介绍后应看到 `[证据]`、`[疑点]`、`[追问]`、`[风险]` 或 `[亮点]` 标签。
6. 在 `AI 应用评审` 场景点击 `生成评分表`，应生成参考总分、维度评分、待验证问题和赛后反馈摘要。
7. 在 `求职面试` 场景点击 `面试复盘`，应生成问题清单和下一轮准备建议。
