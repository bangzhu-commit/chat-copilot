# 「把天聊下去」安装指南

实时对话 AI 副驾——实时听你说话，帮你识别问题、洞察风险、想清楚下一句。

---

## 前提条件

- **Node.js v18+**（下载：https://nodejs.org ，装 LTS 版本就行）
- **Chrome 浏览器**（需要用麦克风，Chrome 兼容性最好）

---

## 需要申请两个 API 密钥

### 1. OpenRouter（AI 提示能力）

1. 打开 https://openrouter.ai ，注册账号
2. 进入 Keys 页面 → **Create Key** → 复制保存
3. 充值几块钱就够用很久（我们用的 DeepSeek 模型非常便宜）

### 2. 火山引擎（语音识别）

1. 打开 https://www.volcengine.com ，注册账号
2. 完成**实名认证**（必须，否则无法开通服务）
3. 进入控制台 → 找到**豆包语音** → 创建应用
4. 勾选「**流式语音识别模型2.0**」
5. 拿到 **APP ID** 和 **Access Token**，复制保存

> 💡 新用户有免费试用包，够测试用。

---

## 安装步骤

### ① 解压 zip

把 `把天聊下去-分发包.zip` 解压到任意目录。

### ② 填写 API 密钥

用文本编辑器打开项目里的 **`.env`** 文件，把三个占位符替换成你自己的 Key：

```
LLM_API_KEY=在这里填入你的OpenRouter API Key
ASR_APP_ID=在这里填入你的火山引擎APP ID
ASR_ACCESS_TOKEN=在这里填入你的火山引擎Access Token
```

其他配置不用动。

### ③ 安装依赖

打开终端（Mac 按 `Cmd+空格` 搜"终端"），运行：

```bash
cd 你的项目目录路径
npm install
```

### ④ 启动

两种方式任选：

- **双击** `启动.command` 文件
- 或者在终端运行 `npm start`

### ⑤ 打开浏览器

访问 **http://localhost:3000** ，搞定。

---

## 使用说明

1. 点击 **「开始转写」** → 浏览器会请求麦克风权限，允许即可
2. 正常说话，页面会实时显示语音转文字
3. 选择合适场景：直播主持、访谈采访、招聘面试（面试官）、求职面试、销售谈判、相亲约会、AI 应用评审、口播录制、培训教学
4. **停顿约 2 秒**，AI 会自动生成追问建议、实时洞察或回答提示；也可以点 **「手动追问」**
5. 如果想要更深的问题，可以点 **「深度追问」**；直播主持、访谈采访、AI 应用评审、销售谈判、招聘面试（面试官）会显示这个按钮
6. **「自动深度」默认关闭**。打开后，AI 会在对方说完一整段、说话人切换或长停顿后，自动生成更深的问题
7. 右侧转写区上方会显示角色映射状态；如果火山 ASR 返回说话人信息，会出现「说话人 1 / 说话人 2」下拉框，可按场景改成「嘉宾 / 主持人」「客户 / 我方」等角色
8. 可以上传**资料**，AI 会结合资料内容给出更贴合的建议

普通追问适合救场和接话；深度追问适合对方刚讲完一整段后，用来追证据、追反差、追没展开的关键点。现场不确定时，先用手动深度追问；确认节奏稳定后，再打开自动深度。

---

## 上传资料怎么准备

销售谈判可以上传 `.txt` / `.md`：

```markdown
客户/嘉宾：姓名、角色、公司、权限范围
我方目标：希望达成什么下一步
底线：价格、交付、付款、不可承诺事项
方案：产品卖点、案例、报价区间
已知信息：痛点、预算、竞品、历史沟通
```

求职面试可以上传 `.txt` / `.md`：

```markdown
目标岗位：岗位 JD 和关键要求
候选人资料：简历、项目经历、优势
重点素材：想强调的项目、数据、协作角色
表达边界：不能夸大的经历、还没做过的能力
目标公司：业务、产品、面试岗位背景
```

相亲约会可以上传 `.txt` / `.md`：

```markdown
对方基本信息：职业、城市、兴趣、介绍人提供的信息
自己想展示的真实信息：兴趣、生活方式、价值观
本次目标：轻松认识、判断是否继续约、了解生活节奏
禁区：不想聊的话题、不要显得太功利的问题
```

相亲约会场景更适合线上语音相亲、相亲前演练、相亲后回顾，或者双方知情的实时转写场景。它会提醒破冰、共鸣、自然追问和边界，不会生成 PUA、施压或诱导隐私的话术。

AI 应用评审可以上传 `.txt` / `.md`：

```markdown
项目名称：
解决的问题：面向谁，为什么重要
核心功能：最重要的 3 个功能
AI 用在哪里：问答、总结、生成、识别、检索或自动化
当前完成度：Demo、可试用、已上线或稳定使用
效果证据：数据、案例、使用反馈、节省时间
风险和边界：隐私、权限、错误输出、成本和安全
评委关注点：希望评委重点看什么
```

AI 应用评审场景适合作为评委助理，不直接决定名次。项目结束后可以点击 **「生成评分表」**，AI 会基于完整转写和项目资料生成参考总分、维度评分、待验证问题和赛后反馈摘要。

求职面试结束后，可以点击 **「面试复盘」**，AI 会基于完整转写整理问题清单、薄弱项和下一轮准备清单。

---

## 常见问题

**Q：一直显示"连接中"，卡住不动？**
→ 检查 `.env` 里的 ASR 密钥是否正确。确认火山引擎后台已经开通了「流式语音识别模型2.0」。

**Q：LLM 报错 403？**
→ OpenRouter 上部分模型有地区限制。建议使用 `deepseek/deepseek-chat`，没有这个问题。

**Q：没有声音 / 不转写？**
→ 检查 Chrome 是否授权了麦克风权限（地址栏左边的锁图标 → 网站设置 → 麦克风 → 允许）。

---

有问题随时找我。
